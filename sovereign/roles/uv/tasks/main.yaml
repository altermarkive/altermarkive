---
- name: uv - Installation
  command: /bin/sh -c "((curl -LsSf https://astral.sh/uv/install.sh | sh); uv self update) || true"

- name: uv - Clean
  cron:
    name: uv-clean
    hour: "0"
    minute: "0"
    job: uv cache clean
