name: Automation Script

on: [push]

jobs:
  pipeline:
    name: Pipeline
    runs-on: ubuntu-18.04
    env:
      DOCKERHUB_USER: ${{ github.actor }}
    steps:
      - name: Initiating
        uses: actions/checkout@v1
      - name: Building & Publishing
        run: |
          echo ${{ secrets.DOCKER_REGISTRY_TOKEN }} | docker login https://docker.pkg.github.com --username $GITHUB_ACTOR --password-stdin
          /bin/sh automation/build-and-publish.sh
